jwt:
  public-key-path: /tmp/analytics-service-test-keys/public.pem

kafka:
  topic:
    playback-events: playback-events-test
  consumer:
    group-id: analytics-service-test

spring:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}
